# 개발 일지 - 2026-03-03

## 오늘의 작업 요약

미커밋 변경사항 정리 & 배포, TanStack Query 완전 마무리, AI 기능 검증

---

## 1. 커밋 & 배포

쌓여있던 변경사항(595줄 추가)을 4개 논리 커밋으로 정리 후 Vercel 배포.

| 커밋 | 내용 |
|------|------|
| `feat(ai)` | AI 유사 영화 / AI Pick 추천 엔드포인트 |
| `refactor(frontend)` | TanStack Query 전면 적용 (movies, match, my-taste, movies/[id]) |
| `fix(MovieCard)` | LCP 최적화 + 호버 오버레이 색상 강제 |
| `docs` | 개발 일지 + 스킬/훅/커맨드 파일 |

- 프론트 배포: https://jnsquery-reflix-eight.vercel.app ✅

---

## 2. TanStack Query 완전 마무리

### `/people/[id]`
이미 `useQuery` 적용 완료 상태였음 (INDEX.md 오래된 정보). 변경 없음.

### `/profile`
```tsx
// Before: useState(saving) + try/catch
const [saving, setSaving] = useState(false);
const handleMBTIChange = async (mbti: string) => {
  setSaving(true);
  try { await updateMBTI(mbti); }
  finally { setSaving(false); }
};

// After: useMutation
const { mutate: saveMBTI, isPending: saving } = useMutation({
  mutationFn: (mbti: string) => updateMBTI(mbti),
  onError: (error) => console.error("Failed to update MBTI:", error),
});
const handleMBTIChange = (mbti: string) => {
  setSelectedMBTI(mbti);
  saveMBTI(mbti);
};
```

---

## 3. AI 기능 검증

| 엔드포인트 | 결과 |
|---|---|
| `GET /recommendations/ai-similar/550` | 5편 반환 정상 |
| `GET /recommendations/ai-pick` (인증 없음) | 401 정상 |

---

## TanStack Query 적용 현황 (최종)

| 페이지 | 방식 |
|--------|------|
| `/` | `useQuery` |
| `/movies` | `useQuery` + `useInfiniteQuery` (듀얼 모드) |
| `/movies/[id]` | `useQuery` × 3 |
| `/favorites` | `useInfiniteQuery + useMutation` |
| `/ratings` | `useInfiniteQuery + useMutation` |
| `/search` | `useInfiniteQuery` |
| `/my-taste` | `useQuery` × 2 |
| `/match` | `useMutation` |
| `/people/[id]` | `useQuery` ✅ |
| `/profile` | `useMutation` (MBTI) + zustand (auth) ✅ |
